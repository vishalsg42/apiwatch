// Stub — Task 7 replaces this with real URL classification.
import type { Node } from 'ts-morph'
import type { UrlExpr } from '../model.js'

export function classifyUrl(_arg: Node | undefined): UrlExpr {
  return { kind: 'unknown' }
}
